# legacy/uchet-node

Оригинальный архив `auth-module.tar.gz` (Node.js/Express, проект «Учёт»),
оставлен для сверки и истории. Рабочая версия для этого проекта — Django-
приложение `accounts/` в корне репозитория (см. README.md в корне).
Код здесь не используется в сборке/деплое (не упоминается в Dockerfile).
